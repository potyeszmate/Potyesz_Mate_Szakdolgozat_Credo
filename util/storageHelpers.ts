// import AsyncStorage from '@react-native-async-storage/async-storage';
// import EventEmitter from 'react-native/Libraries/vendor/emitter/EventEmitter';

// const storageEmitter = new EventEmitter();

// export const updateLanguage = async (newLanguage: any) => {
//     await AsyncStorage.setItem('selectedLanguage', newLanguage);
//     storageEmitter.emit('languageChanged', newLanguage);
// };

// export const getLanguage = async () => {
//     const language = await AsyncStorage.getItem('selectedLanguage');
//     return language || 'English';  // Default to 'English' if nothing is set
// };

// export default storageEmitter;
